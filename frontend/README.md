# Frontend Workspace

This folder contains the Next.js application for ArchDLoom.

## Planned structure

- `app/` App Router pages
- `components/` UI and feature components
- `lib/` API client and shared frontend utilities
- `public/` static assets

Frontend runtime dependencies will be added in the next frontend bootstrap task (`P1-5`).

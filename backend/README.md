# Backend Workspace

This folder contains the FastAPI service and supporting backend modules for ArchDLoom.

## Planned structure

- `app/` application package (API routes, services, models, workers)
- `tests/` backend unit and integration tests
- `pyproject.toml` Python project and tooling configuration
